import { type Config } from 'prettier';

const prettierConfig: Config = {
    trailingComma: 'es5',
    tabWidth: 4,
    singleQuote: true,
    jsxSingleQuote: true,
    bracketSpacing: true,
    arrowParens: 'avoid',
};

export default prettierConfig;
